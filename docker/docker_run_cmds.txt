docker build -t mytemplate:latest .
docker stop mytemplate && docker rm mytemplate
docker run -d --name mytemplate --restart unless-stopped \
  -p 5000:5000 \
  -v $(pwd)/database.db:/app/database.db \
  --env DATABASE_URL="sqlite:////app/database.db" \
  --env APPNAME_ENV=prod \
  --env FORCE_HTTPS_COOKIES=false \
  mytemplate:latest
docker logs -f mytemplate
